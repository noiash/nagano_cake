<div class="container-fluid">
  <div class="row">
    <div class="col-4 table-info">
      <div class="row">
      </div>
    </div>
    <div class="col-8 table-primary">
      <h>ようこそ、ながのCAKEへ！<br>
      このサイトは、ケーキ販売のECサイトになります。<br>
      会員でない方も商品の閲覧はできますが、<br>
      購入には会員登録が必要になります。
      </h>
      <div class="row">
        <div class="col-12 table-success">
          <h4>新着商品</h4>
            <% @items.each do |item| %>
              <div class="item">
              <%= link_to item_path(item) do %>
              <%= attachment_image_tag item, :image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg" %>
              <% end %>
              <%= item.name %>
              <%= item.price %>
              </div>
              <% end %>
        </div>
      </div>
    </div>
  </div>
</div>