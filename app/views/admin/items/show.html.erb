<div class-"container">
    <div class="d-flex align-items-center">
      <p class="p-3 mb-10 ml-5 bg-info text-white">商品詳細</p>
    </div>

  <div class="row">
  <div class="col-md-3 ml-5">
  <%= attachment_image_tag @item, :image, :fill, 200, 200, format: 'jpeg', fallback: "no_image.jpg" %>
  </div>

  <div class="col-md-">
    <p>商品名</p>
    <p>商品説明</p>
    <p>ジャンル</p>
    <p>税込価格（税抜価格）</p>
    <p>販売ステータス
    <% if @item.is_active == true %>
    <p>販売中</p>
    <% else %>
    <p>販売停止中</p>
    <% end %>
    </p>
    </div>

   <div class="col-md-2">
     <p><%= @item.name %></p>
     <p><%= @item.introduction %></p>
     <p><%= @item.genre.name %></p>
     <p><%= (@item.price * 1.1).floor %>（<%= @item.price %>）円</p>
     <p></p>
   </div>
  </div>



    <div class="text-center">
    <%= link_to "編集する", edit_admin_item_path, class: 'btn btn-success' %>
    </div>


</div>
