<div class-"container">
  <div class="row">
  <div class="col-md-10 offset-md-1">
    <div class="d-flex align-items-center">
      <p class="p-3 mb-10 bg-info text-white">商品詳細</p>
    </div>

  <div class="col-md-3 mr-4">
  <%= attachment_image_tag @item, :image, :fill, 200, 200, format: 'jpeg', fallback: "no_image.jpg", class: 'float-left img-fluid' %>
  </div>

  <div class="col-md-7 justify-content-center">
    <p>商品名<%= @item.name %></p>
    <p>商品説明<%= @item.introduction %></p>
    <p>ジャンル<%= @item.genre.name %></p>
    <p>税込価格（税抜価格）<%= (@item.price * 1.1).floor %></p>
    <p>販売ステータス
    <% if @item.is_active == true %>
    <p>販売中</p>
    <% else %>
    <p>販売停止中</p>
    <% end %>
    </p>
    <div class="text-center">
    <%= link_to "編集する", edit_admin_item_path, class: 'btn btn-success' %>
    </div>
  </div>

  </div>
  </div>
</div>