<div class="container">
  <div class="row">
　　<div class="col-md-10 offset-md-1">
  　　<div class="d-flex align-items-center">
  　　<p class="p-3 mb-10 bg-info text-white">注文履歴一覧</p>
  　　</div>

  　<table>
      <thead>
        <tr>
          <th>注文日時</th>
          <th>購入者</th>
          <th>注文個数</th>
          <th>注文ステータス</th>
        </tr>
      </thead>
      <% @orders.each do |order| %>
      <tbody>
        <tr>
          <td>
            <%= link_to edit_admin_order_path(order) do %>
            <%= order.created_at %>
            <% end %>
            </td>
          <td><%= order.customer.full_name %></td>
          <td>
            <% @order_details.each do |order_detail| %>
            <%= order_detail.amount %>
            <% end %>
          </td>
          <td><%= order.status %></td>
        </tr>
      </tbody>
      <% end %>
  　</table>