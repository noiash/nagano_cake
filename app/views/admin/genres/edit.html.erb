<div class="container">
  <div class="row">
    <div class="col">
      <p>ジャンル名</p>
      <%= form_with model: @genre, url: admin_genres_path, local: true do |f| %>
　     <%= f.text_field :name %>
　     <%= f.submit "変更を保存", class: 'btn btn-success' %>
      <% end %>
    </div>
  </div>
</div>